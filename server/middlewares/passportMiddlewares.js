import passport from "passport";

function passportMiddlewareWith (req,res,next,error){
    //Este middleware se usa despues de authenticate entonces ya tengo req.user... miro el role y deepede el rol autorizo o no
}